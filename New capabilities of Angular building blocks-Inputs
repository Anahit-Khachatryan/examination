Required input

@Input() projectId: number; եթե գրենք այսպես, ու input-ին default value չտանք, error կտա Property 'projectId' has no initializer and is not definitely assigned in the constructor. Եթե input required են, այժմ գրում ենք այսպես, առաջվա
նման error hanleingner-ի և այլն
To address this, Angular v16 added required input properties. From now on, we
can make any inputs required, and the compiler will ensure those values are provided
wherever necessary. As mentioned, it will be done in compile-time and will immediately show errors in the template where we use the component but omit a required
input. @Input({required: true}) projectId!: number;
Now if we try to use this without providing the input’s value, we will receive an error.

transform input - 
երբ օրինակ որպես input կարանք ստանանք ֆայլերի տեսակներ, թե որոնք են հնարավոր upload անել, մեր component-ում եթե առանց էս transformi անեինք, պետքաա ունենայինք getter ֆունկցիա որը input.split(,), հետո 
ֆայլՍելեքթի ժամանակ այդ ֆ-ն օգտագործեինք, բայց սա ունենում է խնդիրներ - this will trigger change detection multiple times as we are using the getter, which is essentially a function, in the template - էջ 80-մանրամասն
@Input({
transform: (value: string) => value.split(','),
 }) 

numberAttribute and booleanAttribute
@Input({transform: booleanAttribute}) booleanProperty = false;
@Input({transform: numberAttribute}) numberProperty = 0

Activated route-i հետ կապված
Օրինակ որ լիստ էինք ունենում, ու գնում էինք դիթեյլս, ActivatedRoute-ին էինք Subscribe անում, id-ն get անում, ու համապատասխան փոփոխությունը անում։ Հիմա հնարավորություն կա withComponentInputBinding ավելացնել, և routing-ում եթե գրել ես օրինակ id, ունենում ես նույնանուն input
 @Input({transform: numberAttribute}) id!: numbe և ինքը արժեքը վերցնում է
provideRouter(routes, withComponentInputBinding()),
The binding only works for routed components. Եթե ունենք կոմպոնենտ որ ռենդեռա լինում մյուս կոմպոնենտի մեջ, սա չի աշխատի

Inputs for dynamic component
Անընդհատ app-loader component-ի մեջ գրելու փոխարեն, գրում ենք loaderComponent structural directive - սա խառնա կարդալ 85-րդ էջում
from Angular v16.2, there is a new way of passing inputs to components dynamically rendered using *ngComponentOutlet
<ng-container *ngComponentOutlet="actionsSection; inputs: {candidateId: 
candidate.id}"></ng-container>

 Host directives
 Sometimes several directives are often used together, and it makes perfect sense to us to be able to create a parent directive that will call the other ones, instead of constantly spelling them out, 
or if we have a directive that uses the functionality of another directive while adding some of its own; but again, there was no real and official way of doing this. 
The good news is, starting from Angular v15, a new concept of host directives has been added to the framework that allows us to add directives to another directive when the latter is applied. Let us see it in action
